//use rand::prelude::Rng;
//use alghlib::*;

/*struct Percolation{
    open_sites: usize,
    open_list: Vec<bool>,
    grid: Wqupc,
    full: Wqupc,
    length: usize

}

impl Percolation{
    pub fn percolation(n: usize) -> Self{
        let total_sites : usize =  n*n;

        if n <= 0{
            panic!("[] N must be greater than 0"); 
        }
        else {
            return Percolation { 
                grid : alghlib::Wqupc::new(total_sites+2),
                full : alghlib::Wqupc::new(total_sites+1),
                open_sites : 0,
                length : n,
                open_list : Vec::with_capacity(total_sites+2)
            };
        }
    }

    pub fn block_sites(&mut self){
    }
}*/

fn main() {
    println!("Hello, world!");
}


